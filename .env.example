# What Does This Box Do - Configuration
# Copy this file to .env and fill in your credentials

# =============================================================================
# GitLab Configuration
# Used to scan repositories and compare with paths found on analyzed servers
# =============================================================================
GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
# Optional: Limit scanning to specific groups
GITLAB_GROUPS=infrastructure,devops,applications

# =============================================================================
# Harbor Registry Configuration
# Used to compare running containers with registry images
# =============================================================================
HARBOR_URL=https://harbor.example.com
HARBOR_USERNAME=admin
HARBOR_PASSWORD=your-harbor-password
# Optional: Robot account token (alternative to username/password)
# HARBOR_ROBOT_TOKEN=your-robot-token

# =============================================================================
# vCenter/vSphere Configuration
# Used to gather VM details, resource allocation, and cluster info
# =============================================================================
VCENTER_HOST=vcenter.example.com
VCENTER_USERNAME=administrator@vsphere.local
VCENTER_PASSWORD=your-vcenter-password
# Optional: Specific datacenter to query
VCENTER_DATACENTER=DC1

# =============================================================================
# Proxmox Configuration
# Used to gather VM/container details from Proxmox clusters
# =============================================================================
PROXMOX_HOST=proxmox.example.com:8006
PROXMOX_USERNAME=root@pam
PROXMOX_PASSWORD=your-proxmox-password
# Optional: API token (alternative to username/password)
# PROXMOX_TOKEN_ID=user@pam!tokenid
# PROXMOX_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# =============================================================================
# Cloud Provider Credentials (for cost estimation APIs)
# =============================================================================
# AWS (optional - uses public pricing if not set)
# AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
# AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AWS_REGION=us-east-1

# Azure (optional - uses public pricing API)
# No credentials needed for Azure Retail Prices API

# GCP (optional - uses public pricing if not set)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
