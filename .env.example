# WhatDoesThisBoxDo - Environment Configuration
# Copy this file to .env and update values for your environment

# =============================================================================
# Application Settings
# =============================================================================

# Flask secret key (REQUIRED - generate a random string)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-use-random-64-char-hex-string

# Enable debug mode (set to 'true' only for development)
DEBUG=false

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL credentials
POSTGRES_USER=whatdoesthisboxdo
POSTGRES_PASSWORD=change-me-use-strong-password
POSTGRES_DB=whatdoesthisboxdo

# =============================================================================
# Security
# =============================================================================

# Encryption key for storing credentials (REQUIRED - generate a random string)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=change-me-use-random-44-char-base64-string

# =============================================================================
# SSH Configuration
# =============================================================================

# Path to SSH keys on host (mounted read-only into container)
SSH_KEY_PATH=~/.ssh

# =============================================================================
# External Service Configuration (Optional)
# These can also be configured via Admin > Environment Variables
# =============================================================================

# GitLab integration
# GITLAB_URL=https://gitlab.example.com
# GITLAB_TOKEN=your-gitlab-token

# Harbor registry integration
# HARBOR_URL=https://harbor.example.com
# HARBOR_USERNAME=your-username
# HARBOR_PASSWORD=your-password

# vCenter integration
# VCENTER_HOST=vcenter.example.com
# VCENTER_USER=your-username
# VCENTER_PASSWORD=your-password

# Proxmox integration
# PROXMOX_HOST=proxmox.example.com
# PROXMOX_USER=your-username
# PROXMOX_PASSWORD=your-password
